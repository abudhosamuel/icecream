import React from 'react';

const AdminPanel = () => {
    return (
        <div className="admin-panel">
            <h2>Admin Panel</h2>
            <p>Manage flavors, orders, and more.</p>
            {/* Add navigation to Flavor Management, Order Management, etc. */}
        </div>
    );
};

export default AdminPanel;
